/** @type {import('next').NextConfig} */

const nextConfig = {
  reactStrictMode: true,
  i18n: {
    defaultLocale: "en-US",
    locales: ["en-US", "ni-NL", "hi-IN"],
  },
};

export default nextConfig;
